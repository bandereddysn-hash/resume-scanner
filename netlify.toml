[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA fallback (Netlify still routes `/.netlify/functions/*` to functions first)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
